\documentclass{article}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{graphicx}
\usepackage{wrapfig}
\usepackage{hyperref}
\usepackage{minted}
\usepackage{xcolor}
\usepackage{caption}

\graphicspath{{build/media/}}
\definecolor{grey}{rgb}{.95, .95, .95}
\definecolor{mygreen}{rgb}{.6 .95 .1}
\pagecolor{yellow!4}

\title{Introduction to computing for physicists}
\author{Your dearest, green}

\begin{document}
\maketitle

\pagebreak

\input{src/tex/intro.tex}

\pagebreak

\input{src/tex/repr.tex}

\pagebreak

\subsection{Exercises}
For the following exercises read the instructions, draw the flow diagram,
apply reductions if needed and implement the solution in C syntax.

\paragraph{Scale a row}
Given a row of floats $R[n]$ of size $n$ and a factor $fac$, implement a program
to scale each column entry by the same factor (row scaling)
\begin{minted}[bgcolor=mygreen!10]{c}
  void row_scale(int n, float R[n], float fac);
\end{minted}

\paragraph{Add a row}
Given a two rows of floats $R0[n]$ and $R1[n]$ of size $n$ and a factor $fac$,
implement a program that adds together adjacent columns of $R0[n]$ and $R1[n]$
the latter being scaled by a factor of $fac$.
\begin{minted}[bgcolor=mygreen!10]{c}
  void row_add(int n, float R0[n], float R1[n], float fac);
\end{minted}

\paragraph{Swap a row}

\paragraph{Print a matrix}
Given a matrix $M[m][n]$ of size $m \times n$, implement a program that prints
the matrix onto the screen.
\begin{minted}[bgcolor=mygreen!10]{c}
  void matrix_print(int m, int n, float M[m][n]);
\end{minted}
keep in mind that to print something like
\begin{minted}[bgcolor=mygreen!10]{text}
  [1.00 6.67]
  [3.14 6.96]
\end{minted}
the code is the following
\begin{minted}[bgcolor=mygreen!10]{c}
  printf("[%.2f ", 1.00);
  printf("%.2f]\n", 6.67);
  printf("[%.2f ", 3.14);
  printf("%.2f]\n", 6.69);
\end{minted}

\pagebreak

\subsection{Solutions}
\paragraph{Scale a row}
\begin{minted}[bgcolor=mygreen!10]{c}
  void row_scale(int n, float R[n], float fac) {
    for (int j = 0; j < n; j = j + 1) {
      R[j] = fac * R[j];
    }
    return;
  }
\end{minted}

\paragraph{Add a row}
\begin{minted}[bgcolor=mygreen!10]{c}
  void row_add(int n, float R0[n], float R1[n], float fac) {
    for (int j = 0; j < n; j = j + 1) {
      R0[j] = R0[j] + fac * R1[j];
    }
    return;
  }
\end{minted}

\paragraph{Swap a row}
\begin{minted}[bgcolor=mygreen!10]{c}
  void row_swap(int n, float R0[n], float R1[n]) {
    float tmp = 0.0;
    for (int j = 0; j < n; j = j + 1) {
      tmp = R0[j];
      R0[j] = R1[j];
      R1[j] = tmp;
    }
    return;
  }
\end{minted}

\paragraph{Print a matrix}
\begin{minted}[bgcolor=mygreen!10]{c}
  #include <stdio.h>

  void matrix_print(int m, int n, float M[m][n]) {
    for (int i = 0; i < m; i = i + 1) {
      printf("[");
      for (int j = 0; j < n - 1; j = j + 1) {
        printf("%.2f ", M[i][j]);
      }
      printf("%.2f]\n", M[i][n-1]);
    }
    printf("\n");
    return;
  }
\end{minted}

\pagebreak

\subsection{Row reduction algorithm}
The row reduction (also know as gaussian elimination) algorithm, is used to
modify the form of a matrix into upper triangular without modifying its
kernel. This algorithm is used to solve linear systems of equations.
\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{flow-diagram-informal.png}
  \caption{
    This is the (informal) flow diagram of the row-reduction
    algorithm for matrices.
  }
  \label{fig:flow-diagram-informal}
\end{figure}

\paragraph{Implementation}
\begin{minted}[bgcolor=mygreen!10]{c}
  int main(void) {
    float M[3][4] = {
      {0, 2, 3, 4},
      {1, 7, 7, 7},
      {9, 8, 7, 0}
    };
    row_reduction(3, 4, M);
    matrix_print(3, 4, M);
    return 0;
  }
\end{minted}

\input{src/tex/code/row_reduction_bad.tex}

\pagebreak

\paragraph{Optimizations}
After going through the hastle of translating the abstract algorithm
represented by a diagram into the C language syntax and verifying
its expected behaviour, one might be left unsatisfied and have some
of these questions in mind:
\begin{itemize}
\item
  \textit{The diagram is very simple, the code is very ugly. But it works
  great without any problems so its fine right?} It is until it isn't.
  Computer science tells us that there is no way of catching certain
  errors prior to running the program. Ergo write everything with the
  intention of making it easier to fix when it will break.
\item
  \textit{What has been just done can be applied to any language. Is the only
  advantage of C that of making it faster?}
  Although the fastest programs are written in C, that doesn't imply that
  writing any program in C will make it fast.
  The sole dictator of a program's speed is the hardware and the algorithm.
  The race between languages is a race where everyone gets the same car and
  are is only allowed to remove things from it. The C language doesn't care
  about its driver's well being (you), it just wants to win the race:
  no carpets, no seats, no airbags, no safety, no spedometer...
  If you are the best driver you will definitely win the race, otherwise
  you will just die (the program won't run and that will make you want
  to be not so nice towards yourself and other).
\item
  \textit{If my focus isn't performance is it worth learning C?}
  Yes it is. Following the previous answer, it has no other structure other
  than the absolutely necessary one.
\end{itemize}

In the implementation diagram \ref{fig:flow-diagram-formal} pay attention to
the row swapping section. There is obviously some useless repetition carrying
over from checking the possibility of doing a row swap at all, more importantly
it introduces a hanging terminating condition (which shouldn't resolve but is
still considered bad practice). It is better to remove it all together and
adjust it so that the improved \ref{fig:flow-diagram-formal-1}

\begin{figure}[p]
  \centering
  \includegraphics[height=.9\textheight]{row-reduction-diagram-formal.png}
  \caption{Formalized diagram of the previous row reduction algorithm implementation.}
  \label{fig:flow-diagram-formal}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[height=.9\textheight]{row-reduction-diagram-formal-1.png}
  \caption{Formalized diagram of the previous row reduction algorithm implementation.}
  \label{fig:flow-diagram-formal-1}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[height=.9\textheight]{row-reduction-diagram-formal-2.png}
  \caption{Formalized diagram of the previous row reduction algorithm implementation.}
  \label{fig:flow-diagram-formal-2}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[height=.9\textheight]{row-reduction-diagram-formal-3.png}
  \caption{Formalized diagram of the previous row reduction algorithm implementation.}
  \label{fig:flow-diagram-formal-3}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[height=.9\textheight]{row-reduction-diagram-formal-4.png}
  \caption{Formalized diagram of the previous row reduction algorithm implementation.}
  \label{fig:flow-diagram-formal-4}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[height=.9\textheight]{row-reduction-diagram-formal-5.png}
  \caption{Formalized diagram of the previous row reduction algorithm implementation.}
  \label{fig:flow-diagram-formal-5}
\end{figure}

\pagebreak

\input{src/tex/code/row_reduction_good.tex}

\pagebreak

\input{src/tex/data.tex}

\pagebreak

\section{Biblio}
\begin{itemize}
\item ``Informatica: Fondamenti Culturali e Tecnologici 1986'' by \textit{Michele Pellerey}
\item ``Introduction to Algorithms (third edition) 2009'' by \textit{Thomas H. Cormen}
\end{itemize}

\end{document}
